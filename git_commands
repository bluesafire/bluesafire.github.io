git commands
-------------
git clone <url>
git branch
git status
git log

git add 
git diff
git show <commit id>
git commit -m "comments "
git push 

git config --list
git config --global user.name "myursername"
git config --global user.email "myuseremail"
git config --global credential.helper 'store --file ~/.my-credentials'
